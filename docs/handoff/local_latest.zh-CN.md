[English](local_latest.md) | [简体中文](local_latest.zh-CN.md)

# 本地交接

- 更新时间：2026-08-08（Asia/Shanghai）
- Git 提交：`8612de6`（核心）、`6c7cdbf`（ROS 闭环）、`adc89a7`（安全）、
  `e6883b1`（构建）、`2845421`（双语文档）
- 分支：`work/local`（已推送并跟踪 `origin/work/local`）
- Schema 版本：已接受 `0.1.0`
- 磁盘：`/` 约 4.1 GiB 可用；`/home` 约 24 GiB 可用
- 输入：合成和录制模式已通过同一 launch 验证；未检测到摄像头

## 已完成

- 阅读本地指南、服务器指南和完整 29 页实施清单。
- 审计初始工作区和核心本地命令。
- 建立仓库记忆、ADR、交接、Git 工作流和安全规则。
- 运行只读本地 doctor，并把已验证 M0 基线推送到 GitHub。
- 通过 `work/local` 建立受保护的本地集成路径。
- 定义并编译四个 ROS2 接口，接受坐标系/变换契约。
- 增加合成发布器、ROS 无关校验器、ROS2 校验/状态节点，以及普通和低置信度
  smoke 探针。
- 增加录制 JSON schema/播放器、校验和锁定五帧固定样例、统一输入模式 launch、短
  rosbag 采集和独立双次回放检查器。
- 增加严格 SE(3)、标定加载、单位/坐标系校验和合成固定样例点变换。
- 增加手掌局部尺度归一化、显式左右手映射、低通与 One Euro 滤波、标定腕部位姿、
  五个有界原型手指目标、工作空间/速率限制和关闭失败的过期输入 watchdog。
- 增加 `/robot/target`、`/episode/record`、序列对齐、JSONL 持久化、完整 launch，以及
  有限合成、录制和数据源中断验收脚本。
- 增加 22 份简体中文文档对应版本、双向语言切换和自动化本地化完整性测试。

## 本地验证

- ROS2 Humble 与 colcon 命令可用。
- 59 项单元/契约/文档测试通过；三个 ROS2 包全部构建成功。
- 最终 M2 smoke：15 个有效、0 个无效、0 个丢失、30 Hz，最后采样延迟 3.21 ms。
- 故障 smoke：0 个有效、9 个无效，状态 `DEGRADED`，错误 `low_confidence`。
- 录制 smoke：恰好五帧，序列 0–4，0 个无效、0 个丢失。
- Bag smoke：5 条原始 SQLite3 消息；两个全新校验器都精确接受序列 0–4。
- 标定：录制腕点 `[0.0, 0.06, 0.45]` 映射到 robot-base
  `[0.5, 0.06, 1.25]`；逆变换/组合/单位错误检查通过。
- 滤波评估：确定性抖动 RMS 从 `0.004` m 降到 `0.000424675` m，比率 `0.106169`。
- 原型合成 smoke：20 条完整、配对、有效记录和经过校验和验证的 JSONL。
- 原型录制 smoke：五条完整记录，精确序列 0–4。
- Watchdog smoke：观察到 29 个有效目标后，恰好产生一个 `STALE_INPUT` 无效目标。
- 未安装模型、数据集、虚拟环境或系统包。

## 已上传文件与校验和

没有外部上传。受跟踪固定样例 SHA256 为
`73dac677df09df1ab6d82b78710911025cf8b34d0c4e84bc34f42c185802c725`；
37,876 字节本地 bag 及其哈希记录在 M3 run manifest 中。

## 服务器所需行动

在大型安装或仿真工作前，完成服务器只读审计并填写 `server_latest.md`。

## 已知问题

- 根分区空间余量较低，禁止本地安装依赖。
- 摄像头可用性尚未解决，但不阻塞合成输入。
- 沙箱禁止 UDP 接口枚举并产生 Fast DDS 警告；本地 ROS 传输仍通过。应在沙箱外重新
  检查普通 DDS 网络。
- 五个 `prototype_*_flexion` 目标是 CPU 测试接收器数值，不是真实机器人或仿真器
  命令。资产专用 IK 和碰撞安全仍属于服务器工作。

## 下一步

运行服务器只读审计，冻结一个机器人/手部资产 manifest，再根据不变的共享契约实现
服务器 IK 和 Isaac Sim 执行。
