# Local data area

This directory is intentionally excluded from Git except for this policy file. Store only the
current short samples and small generated results here. Keep local samples below 2 GiB, attach a
schema-versioned manifest and SHA256 to every transfer, and remove older samples only after the
server copy has been verified.
